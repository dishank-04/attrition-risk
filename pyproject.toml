[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "retail_churn_engine"
version = "0.1.0"
description = "Retail Churn Prediction"

[tool.setuptools]
packages = ["src"]